# ********************************************************************* 
# ********************************************************************* 
# Ingress de cri-prod dev
# ********************************************************************* 
# ********************************************************************* 
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: "cri-prod"
  namespace: "cri-prod"
  annotations:
    kubernetes.io/ingress.class: "alb"
    alb.ingress.kubernetes.io/group.name: "cmp1"
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS":443}]'
  labels:
    app-name: cri-prod
spec:
  rules:
    - host: api-emisor-cri.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "api-emisor"
            servicePort: 8080
    - host: core-emisor-cri.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-emisor"
            servicePort: 8080       
    - host: core-movil-operacion-cri.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-movil-operacion"
            servicePort: 8080         
    - host: core-movil-registro-cri.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-movil-registro"
            servicePort: 8080       
    - host: core-movil-transaccional-cri.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-movil-transaccional"
            servicePort: 8080             
    - host: core-transaccional-cri.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-transaccional"
            servicePort: 8080
    - host: core-workplacebank-loans-cri.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-workplacebank-loans"
            servicePort: 8080                              
    - host: portal-emisor-cri.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "portal-emisor"
            servicePort: 8080