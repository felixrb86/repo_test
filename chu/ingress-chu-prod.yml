# ********************************************************************* 
# ********************************************************************* 
# Ingress de chu-prod dev
# ********************************************************************* 
# ********************************************************************* 
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: "chu-prod"
  namespace: "chu-prod"
  annotations:
    kubernetes.io/ingress.class: "alb"
    alb.ingress.kubernetes.io/group.name: "cmp1"
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS":443}]'
  labels:
    app-name: chu-prod
spec:
  rules:
    - host: api-emisor-chu.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "api-emisor"
            servicePort: 8080
    - host: core-emisor-chu.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-emisor"
            servicePort: 8080       
    - host: core-movil-operacion-chu.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-movil-operacion"
            servicePort: 8080         
    - host: core-movil-registro-chu.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-movil-registro"
            servicePort: 8080       
    - host: core-movil-transaccional-chu.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-movil-transaccional"
            servicePort: 8080             
    - host: core-transaccional-chu.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-transaccional"
            servicePort: 8080
    - host: core-workplacebank-loans-chu.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-workplacebank-loans"
            servicePort: 8080                              
    - host: portal-emisor-chu.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "portal-emisor"
            servicePort: 8080
    - host: core-disbursement-chu.cobre.co
      http:
        paths:
        - path: /*
          backend:
            serviceName: "core-disbursement"
            servicePort: 8080            